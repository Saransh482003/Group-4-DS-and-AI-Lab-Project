# Submission: Custom Video Eval (Headless)
# Uses a locally downloaded custom video under data_cache/Custom_Videos/.

mode: "dataset_eval"
source_path: "data_cache/Custom_Videos/indore_test_divyang_vid.mp4"

# Adjust as needed for final run.
max_frames: 1000
stride: 2

execution_mode: "sequential"
enable_tts: true
show_windows: false
save_annotated_video: true

# Run Command:
# python -m app.main --runner-config e2e_runner_configs/submission_custom_video.yaml

