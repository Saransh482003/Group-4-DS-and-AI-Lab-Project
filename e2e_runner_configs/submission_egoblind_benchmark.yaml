# Submission: EgoBlind Benchmark (Headless)
# Runs the benchmark sweep on EgoBlind with consistent limits for reporting.

mode: "benchmark"
dataset: "egoblind"

max_frames: 300
stride: 5

enable_tts: true
show_windows: false
save_annotated_video: true

# Run Command:
# python -m app.main --runner-config e2e_runner_configs/submission_egoblind_benchmark.yaml

